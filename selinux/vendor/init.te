allow init self:capability sys_module;
allow init tmpfs:lnk_file create;
allow init rild:process noatsecure;
allow init debugfs:dir mounton;
allow init sysfs_bluetooth_writable:file setattr;
allow init sysfs_display:file { open setattr write read };
allow init sysfs_radio:file setattr;
allow init proc:file { open write };
allow init sysfs:file { setattr };
allow init sysfs_batteryinfo:file setattr;
allow init sysfs_cpuinfo:file { setattr open read write };
allow init su_exec:file { execute getattr open read };
allow init sysfs_lmkd:file setattr;
allow init sudaemon:process { noatsecure rlimitinh siginh transition };
allow init sysfs_cpufreq:file setattr;
allow init self:capability sys_nice;
allow init sysfs:lnk_file setattr;
allow init audio_device:chr_file { ioctl write };
allow init socket_device:sock_file { unlink };
allow init hwservicemanager:binder { transfer call };
allow init system_data_file:file { lock };
allow init gps_data_file:file { lock };
allow init gps_data_file:fifo_file { write };
allow init hidl_base_hwservice:hwservice_manager { add };
allow init radioreset:process noatsecure;
allow init app_data_file:dir { add_name getattr setattr write };
allow init app_data_file:file { getattr open read setattr write };
allow init mnt_user_file:lnk_file read;
allow init sdcardfs:dir { add_name search write};
allow init sdcardfs:file { create open write };
allow init storage_file:lnk_file read;
allow init kernel:security read_policy;
allow init socket_device:sock_file { create setattr };
allow init servicemanager:binder call;
allow init sysfs_cpufreq:file write;
allow init system_server:binder { call transfer };
allow init sysfs_cpufreq:file open;
allow init sysfs_mmc:file { setattr write };
allow init sysfs_mmc:file open;
allow init wifi_firmware_path:file setattr;
allow init bootanim_exec:lnk_file { getattr };
allow init sysfs_add_random:file { open write };
allow init gpu_device:chr_file ioctl;
allow init surfaceflinger:binder call;
allow init surfaceflinger:fd use;
allow init sysfs_usb:file { setattr write };
allow init hal_configstore_ISurfaceFlingerConfigs:hwservice_manager find;
allow init hal_graphics_mapper_hwservice:hwservice_manager find;
allow init block_device:lnk_file setattr;

allow init { domain -lmkd -crash_dump }:process noatsecure;
domain_trans(init, rootfs, kickstart)

